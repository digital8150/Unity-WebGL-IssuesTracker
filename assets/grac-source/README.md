# GRAC source artwork

The Illustrator files in this directory are source artwork for the generated GRAC marks used by the web app. They are optional design inputs, not runtime assets, and are tracked with Git LFS.

Runtime PNGs are kept under `web/public/grac/` and are the files served by the play page. Keep the `.ai` originals here when updating or regenerating those PNGs.
